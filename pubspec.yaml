name: thoxwarroom
description: THOX.ai LLC — War Room mobile command center for ThoxOS, MeshStack, and Hermes Agent operations
version: 4.0.2+139
publish_to: 'none'

environment:
  sdk: ">=3.9.2 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter
  flutter_driver:
    sdk: flutter
  
  # State Management
  flutter_riverpod: ^3.1.0
  
  # Navigation
  go_router: ^17.0.0
  
  # Network & API
  dio: ^5.9.0
  ollama_dart: ^2.4.0
  openai_dart: ^7.0.1
  http_parser: ^4.0.2
  
  
  # Storage
  flutter_secure_storage: ^10.0.0
  hive_ce: ^2.14.0
  hive_ce_flutter: ^2.3.2
  shared_preferences: ^2.3.2
  synchronized: ^3.4.1
  
  # UI Components - Markdown Rendering
  markdown: ^7.3.0
  pdfrx: ^2.4.7
  archive: ^4.0.9
  xml: ^7.0.1
  flutter_inappwebview: ^6.2.0-beta.3
  flutter_svg: ^2.3.0
  # Native Mermaid renderer. These unpublished workspace packages are pinned
  # as submodules under third_party/.
  mermaid_core:
    path: third_party/mermaid/packages/mermaid_core
  mermaid_flutter:
    path: third_party/mermaid/packages/mermaid_flutter
  socket_io_client: ^3.1.2
  yaml: ^3.1.2
  
  
  
  # Modern Animations
  flutter_animate: ^4.5.0
  stupid_simple_sheet: ^0.9.1+1
  
  # Platform Features
  vad: ^0.0.8
  record: ^6.2.0
  audio_session: ^0.2.2
  permission_handler: ^12.0.1
  image_picker: ^1.2.3
  # Retained on beta: latest stable 11.0.2 conflicts with share_plus via win32 as of 2026-06-10.
  file_picker: ^12.0.0-beta.5
  flutter_sharing_intent: ^2.0.4
  flutter_image_compress: ^2.5.0
  path_provider: ^2.1.4
  
  # Utilities
  path: ^1.9.0
  uuid: ^4.6.0
  crypto: ^3.0.3
  package_info_plus: ^10.2.1
  url_launcher: ^6.3.0
  intl: ^0.20.2

  # Icons & Theming
  cupertino_icons: ^1.0.8
  json_annotation: ^4.12.0
  freezed_annotation: ^3.0.0
  wakelock_plus: ^1.4.0
  share_plus: ^13.2.1
  riverpod_annotation: ^4.0.0
  flutter_local_notifications: ^22.1.0
  connectivity_plus: ^7.3.0
  flutter_cache_manager: ^3.4.1
  http: ^1.5.0
  flutter_callkit_incoming: ^3.1.1
  quick_actions: ^1.1.0
  html_unescape: ^2.0.0
  home_widget: ^0.9.2
  flutter_highlight: ^0.7.0
  just_audio: ^0.10.5
  adaptive_platform_ui: ^0.1.110
  flutter_tex: ^5.2.6
  gaimon: ^1.4.2
  xterm: ^4.0.0
  web_socket: ^1.0.1
  web_socket_channel: ^3.0.3
  cached_network_image_ce: ^4.6.4
  jovial_svg: ^1.1.29
  highlight: ^0.7.0
  geolocator: ^14.0.2
  drift: ^2.34.2
  drift_flutter: ^0.3.1
  fleather: ^1.27.0
  # Document model + markdown/HTML codecs backing the Fleather note editor.
  parchment: ^1.27.0

# Required while mermaid_flutter declares mermaid_core from the hosted source:
# Pub cannot combine that source with ThoxWarRoom's unpublished path package, even
# though both versions are 0.1.0. Remove once the upstream dependency is hosted
# or points to the adjacent workspace package.
dependency_overrides:
  mermaid_core:
    path: third_party/mermaid/packages/mermaid_core

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
  build_runner: ^2.15.1
  freezed: ^3.2.0
  json_serializable: ^6.11.1
  flutter_native_splash: ^2.4.6
  riverpod_generator: ^4.0.0+1
  riverpod_lint: ^3.1.0
  mocktail: ^1.0.4
  checks: ^0.3.1
  collection: ^1.19.1
  pigeon: ^27.2.0
  drift_dev: ^2.34.0
  fake_async: ^1.3.3
  # Used only in tests to mock the microphone permission platform channel.
  permission_handler_platform_interface: ^4.3.0
  plugin_platform_interface: ^2.1.8

flutter:
  config:
    enable-swift-package-manager: false
  uses-material-design: true
  generate: true
  # Localization
  # ARB files live in lib/l10n (configured via l10n.yaml)
  
  assets:
    - THIRD_PARTY_NOTICES.md
    - assets/icons/
    - assets/release_notes/
    - assets/chartjs.min.js
    - assets/flutter_tex.js
    - assets/flutter_tex.css
    - assets/vad/

  fonts:
    - family: Geist Sans
      fonts:
        - asset: assets/fonts/geist/Geist-Regular.ttf
          weight: 400
        - asset: assets/fonts/geist/Geist-Italic.ttf
          weight: 400
          style: italic
        - asset: assets/fonts/geist/Geist-Medium.ttf
          weight: 500
        - asset: assets/fonts/geist/Geist-SemiBold.ttf
          weight: 600
        - asset: assets/fonts/geist/Geist-Bold.ttf
          weight: 700
    - family: Geist Mono
      fonts:
        - asset: assets/fonts/geist/GeistMono-Regular.ttf
          weight: 400
        - asset: assets/fonts/geist/GeistMono-Italic.ttf
          weight: 400
          style: italic
        - asset: assets/fonts/geist/GeistMono-Medium.ttf
          weight: 500
        - asset: assets/fonts/geist/GeistMono-SemiBold.ttf
          weight: 600
        - asset: assets/fonts/geist/GeistMono-Bold.ttf
          weight: 700

flutter_native_splash:
  # Splash background matches the light theme; `color_dark` handles dark mode.
  color: "#FFFFFF"
  color_dark: "#000000"
  # Image to display on the splash screen
  image: assets/icons/icon.png

  
  # Android specific settings
  android_12:
    color: "#FFFFFF"
    color_dark: "#000000"
  
  # Web specific settings
  web: false
    
