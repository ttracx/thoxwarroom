# katex_example

A runnable gallery demo for the [`katex`](https://pub.dev/packages/katex) `Math` widget.

```sh
cd packages/katex/example
flutter run
```

The gallery shows inline and display-mode math, font/size/color options, and graceful error
handling. See [`lib/main.dart`](lib/main.dart).
